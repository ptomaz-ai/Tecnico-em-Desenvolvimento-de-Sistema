programa {
  funcao inicio() {
    real baseMaior,baseMenor, altura, area

    escreva("base Maior:")
    leia(baseMaior)

    escreva("baseMenor:")
    leia(baseMenor)

    escreva("altura:")
    leia(altura)

    area=((baseMaior+baseMenor)*altura)/2
    escreva("área do trapézio:", area)

  }
}
